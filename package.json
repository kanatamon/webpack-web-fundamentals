{
  "name": "webpack-web-fundamentals",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:watch:dev": "yarn build:watch -- --env.env=dev",
    "build:watch:prod": "yarn build:watch -- --env.env=prod",
    "build:watch": "yarn build -- --watch",
    "build:prod:firstplugin": "yarn build:prod -- --env.addons=firstplugin",
    "build:prod:bundleanalyze": "yarn build:prod -- --env.addons=bundleanalyze",
    "build:prod": "yarn build -- --env.env=prod",
    "debug:prod": "yarn debug -- --env.env=prod",
    "build:dev": "yarn build -- --env.env=dev",
    "build:allAddons": "yarn build:prod -- --env.addons=bundleanalyze --env.addons=firstplugin",
    "debug": "node --inspect --debug-brk ./node_modules/webpack/bin/webpack.js",
    "build": "node ./node_modules/webpack/bin/webpack.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "file-loader": "^1.1.5",
    "html-webpack-plugin": "^2.30.1",
    "url-loader": "^0.6.2",
    "webpack": "^3.8.1",
    "webpack-bundle-analyzer": "^2.9.1",
    "webpack-dev-server": "^2.9.4",
    "webpack-merge": "^4.1.1"
  }
}
